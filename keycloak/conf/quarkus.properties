# Keycloak Quarkus Configuration
# These settings are applied at build time and runtime

# HTTP/HTTPS settings
quarkus.http.host=0.0.0.0
quarkus.http.port=8080

# HTTPS settings for client certificate
quarkus.http.ssl.client-auth=REQUEST

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.example.keycloak".level=DEBUG
quarkus.log.category."org.keycloak.services.x509".level=DEBUG

# X.509 Certificate Lookup - Nginx provider configuration
spi.x509cert-lookup.nginx.ssl-client-cert=SSL_CLIENT_CERT
spi.x509cert-lookup.nginx.ssl-cert-chain-prefix=SSL_CLIENT_CERT_CHAIN

# Database connection pool
quarkus.datasource.jdbc.min-size=5
quarkus.datasource.jdbc.max-size=20

# Enable metrics (optional)
# quarkus.micrometer.enabled=true
# quarkus.micrometer.export.prometheus.enabled=true
